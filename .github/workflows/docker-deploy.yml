name: CI/CD for Node.js App

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # Step 3: Build and push Docker image
      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: danielpius60001/Activity11:latest

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push

    steps:
      # Step 4: Deploy (placeholder)
      - name: Deploy to Server
        run: |
          echo "Deploying container to production..."
          # Example (replace with your real commands):
          # docker pull your-docker-username/your-repo-name:latest
          # docker stop my-app || true
          # docker rm my-app || true
          # docker run -d -p 80:3000 --name my-app your-docker-username/your-repo-name:latest
